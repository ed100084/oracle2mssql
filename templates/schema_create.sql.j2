{# Template for schema creation #}
-- Create schema for Oracle package: {{ package_name }}
IF NOT EXISTS (SELECT 1 FROM sys.schemas WHERE name = '{{ schema_name }}')
    EXEC('CREATE SCHEMA [{{ schema_name }}]');
GO
